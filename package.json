{
  "name": "node-picker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "esbuild sample/sample.ts --servedir=sample --outdir=sample/js --bundle --format=esm",
    "build": "esbuild element-picker-ext/src/*.ts --outdir=element-picker-ext --bundle --format=esm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "esbuild": "^0.14.42"
  },
  "author": "",
  "license": "ISC"
}
